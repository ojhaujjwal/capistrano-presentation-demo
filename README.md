capistrano-presentation-demo
=================================


### Build image
* `docker build -t capistrano-presentation .`
* `docker run --rm -t -i capistrano-presentation /sbin/my_init -- bash -l`
